/**
 * Este componente foi consolidado no Shell do entregador como o pill superior de saldo.
 * Mantemos o arquivo para evitar quebras de import, mas ele não renderiza nada visualmente.
 */
export function GanhoHojeCard({ valor }: { valor: number }) {
  return null;
}
